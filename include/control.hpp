#pragma once
#include "hft_types.hpp"

namespace hft::engine {
    /**
     * control.hpp acts as the control-plane interface.
     * Structs (producer_ctrl, consumer_ctrl) are defined in hft_types.hpp 
     * and inherited here via the include above.
     */
}